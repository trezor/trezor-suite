/* @flow */
'use strict';

import React from 'react';

export default (props: {}): React$Element<string> => {
    return (
        <section className="landing">
            connect ERROR
        </section>
    );
}