/* @flow */
'use strict';

import React from 'react';

export default (props: any): any => {
    return (
        <section className="landing">
            localstorage ERROR
        </section>
    );
}