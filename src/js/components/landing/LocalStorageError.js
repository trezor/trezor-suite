/* @flow */
'use strict';

import React from 'react';

export default (props: {}): React$Element<string> => {
    return (
        <section className="landing">
            localstorage ERROR
        </section>
    );
}