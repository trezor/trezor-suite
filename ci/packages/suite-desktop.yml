suite-desktop build mac:
    stage: build
    script:
        - yarn cache clean
        - yarn workspace @trezor/suite-data copy-static-files
        - yarn workspace @trezor/suite-desktop build:mac
        - mv $PACKAGE_PATH_SUITE_DESKTOP/build-electron/Trezor\ Beta\ Wallet-*.zip .
    artifacts:
        expire_in: 1 week
        paths:
            - Trezor Beta Wallet-*.zip

suite-desktop build linux:
    stage: build
    script:
        - yarn cache clean
        - yarn workspace @trezor/suite-data copy-static-files
        - yarn workspace @trezor/suite-desktop build:linux
        - mv $PACKAGE_PATH_SUITE_DESKTOP/build-electron/Trezor\ Beta\ Wallet-*.AppImage .
    artifacts:
        expire_in: 1 day
        paths:
            - Trezor Beta Wallet-*.AppImage

suite-desktop build windows:
    image: electronuserland/builder:wine
    stage: build
    script:
        - yarn cache clean
        - yarn workspace @trezor/suite-data copy-static-files
        - yarn workspace @trezor/suite-desktop build:win
        - mv $PACKAGE_PATH_SUITE_DESKTOP/build-electron/Trezor\ Beta\ Wallet-*.exe .
    artifacts:
        expire_in: 1 day
        paths:
            - Trezor Beta Wallet-*.exe

suite-desktop deploy:
    stage: deploy artifacts, others
    only:
        refs:
            - develop
            - schedules
    variables:
        DEPLOY_DIRECTORY: ${DEPLOY_BASE_DIR}/suite-desktop/${CI_BUILD_REF_NAME}
    script:
        - mkdir -p ${DEPLOY_DIRECTORY}
        - rsync --delete -va Trezor Beta Wallet-*.zip "${DEPLOY_DIRECTORY}/"
        - rsync --delete -va Trezor Beta Wallet-*.AppImage "${DEPLOY_DIRECTORY}/"
        - rsync --delete -va Trezor Beta Wallet-*.exe "${DEPLOY_DIRECTORY}/"
    tags:
        - deploy