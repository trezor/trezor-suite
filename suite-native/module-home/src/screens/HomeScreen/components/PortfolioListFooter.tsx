import { Box } from '@suite-native/atoms';

import { ReferralButton } from './ReferralButton';

export const PortfolioListFooter = () => (
    <Box marginTop="sp64" marginHorizontal="sp16">
        <ReferralButton />
    </Box>
);
