import { Screen } from '@suite-native/navigation';
import { Box, Text } from '@suite-native/atoms';

export const ConnectDeviceScreen = () => {
    const a = null;

    return (
        <Screen>
            <Box>
                <Text>ahoj</Text>
            </Box>
        </Screen>
    );
};
