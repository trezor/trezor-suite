import { LottieAnimation } from '@suite-native/atoms';

import createSecureConnectionLottie from '../assets/create-secure-connection-lottie.json';

export const CreateSecureConnectionAnimation = () => (
    <LottieAnimation source={createSecureConnectionLottie} />
);
