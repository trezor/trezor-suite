import { STORE_TXS } from '../../types/index';
import { notify, getDB } from '../../index';

export const addTransaction = async _transaction => {
    return null;
};

export const addTransactions = async _transactions => {
    return null;
};

export const getTransaction = async (txId: string) => {
    return null;
};

export const getTransactions = async (_accountId?: number, _offset?: number, _count?: number) => {
    return null;
};

export const updateTransaction = async (_txId: string, _timestamp: number) => {
    return null;
};

export const removeTransaction = async (_txId: string) => {
    return null;
};
