{
  "name": "@trezor/flags",
  "version": "1.0.0",
  "author": "Trezor <info@trezor.io>",
  "keywords": [
    "Trezor",
    "trezor-flags"
  ],
  "license": "SEE LICENSE IN LICENSE.md",
  "description": "Small javascript library to work with flags stored in trezor devices",
  "main": "lib/trezor-flags.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint .",
    "clean": "rimraf lib",
    "build": "yarn run clean && npx rollup -c rollup.config.js",
    "type-check": "tsc --project tsconfig.json"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "rimraf": "^2.6.2",
    "rollup": "^0.67.4",
    "rollup-plugin-babel": "^4.0.3",
    "rollup-plugin-cleanup": "^3.0.0",
    "ts-jest": "^24.0.2",
    "yarn": "^1.12.3"
  }
}
