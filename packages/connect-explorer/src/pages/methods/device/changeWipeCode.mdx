import { Callout } from 'nextra/components';

import { MessagesSchema as PROTO } from '@trezor/protobuf';

import { ApiPlayground } from '../../../components/ApiPlayground';
import { CommonParamsLink } from '../../../components/CommonParamsLink';
import { ParamsTable } from '../../../components/ParamsTable';
import changeWipeCode from '../../../data/methods/management/changeWipeCode.ts';

<ApiPlayground
    options={[
        { title: 'Change wipe code', legacyConfig: changeWipeCode[0] },
        { title: 'Advanced schema', method: 'changeWipeCode', schema: PROTO.ChangeWipeCode },
    ]}
/>

export const paramDescriptions = {
    remove: '',
};

## Change wipe code

<Callout type="error">
    **Management command** - this method is restricted to Trezor.io and can't be used in 3rd party
    applications.
</Callout>

This method initiates wipe code change sequence.

```javascript
const result = await TrezorConnect.changeWipeCode(params);
```

### Params

<CommonParamsLink />

#### PROTO.ChangeWipeCode

<ParamsTable schema={PROTO.ChangeWipeCode} descriptions={paramDescriptions} />

### Result

```javascript
{
    success: true,
    payload: {
        message: string
    }
}

```

Error

```javascript
{
    success: false,
    error: {
        message: string // error message
    }
}
```
