import { TronSignTransaction as TronSignTransactionSchema } from '@trezor/connect/src/types/api/tron';

import { ApiPlayground } from '../../../components/ApiPlayground.tsx';
import { CommonParamsLink } from '../../../components/CommonParamsLink.tsx';
import { ParamsTable } from '../../../components/ParamsTable.tsx';
import signTransaction from '../../../data/methods/tron/signTransaction.ts';

<ApiPlayground
    options={[        
        { title: 'Sign TransferContract', legacyConfig: signTransaction[0] },
        {
            title: 'Advanced schema',
            method: 'tronSignTransaction',
            schema: TronSignTransactionSchema,
        },
    ]}

/>

export const paramDescriptions = {
    path: 'minimum length is `5`. [read more](/details/path)',
    ref_block_bytes: 'last 4 bytes of the reference block height, in hex format',
    ref_block_hash: '8th to 16th(exclusive) bytes of the reference block hash(ID), in hex format',
    expiration:
        'expiration time of the transaction which should be no more than 1 day. The unit is milliseconds',
    timestamp: 'timestamp of the transaction. The unit is milliseconds',
    data: 'optional transaction memo',
    fee_limit:
        'maximum fee to consume in a contract related transaction. The unit is sun (1 TRX = 1000000 sun)',
};

## Tron: sign transaction

Sign a given Tron transaction on device. User will be asked to confirm transaction details on Trezor.
The API is compatible with TronWeb transaction object.

Currently supported contract types:

- `TransferContract`
- `TriggerSmartContract`
- `FreezeBalanceV2Contract`
- `UnfreezeBalanceV2Contract`
- `WithdrawExpireUnfreezeContract`

```javascript
const result = await TrezorConnect.tronSignTransaction(params);
```

### Params

<CommonParamsLink />

#### TronSignTransaction

<ParamsTable schema={TronSignTransactionSchema} descriptions={paramDescriptions} />

### Example

Use `TransferContract` to send a TRX transfer

```javascript
const { signature } = await TrezorConnect.tronSignTransaction({
    path: "m/44'/195'/0'/0/0",
    ref_block_bytes: 'e942',
    ref_block_hash: '6394747da9fee421',
    expiration: 1752562632000,
    timestamp: 1752562572000,
    fee_limit: 10000000,
    contract: [
        {
            type: 'TransferContract',
            parameter: {
                value: {
                    owner_address: '41f2cd810c48c401d392ead3c6e1e1cb9f57750a58',
                    to_address: '4141f82674a30ae1328745d08afe2d1a0a24195283',
                    amount: 18123456,
                },
            },
        },
    ],
});
```

Use TronWeb to create the transaction object

```javascript
const transaction = await tronWeb.transactionBuilder.sendTrx(
    'TVDGpn4hCSzJ5nkHPLetk8KQBtwaTppnkr',
    100,
    'TNPeeaaFB7K9cmo4uQpcU32zGK8G1NYqeL',
);
const result = await TrezorConnect.tronSignTransaction({
    path: "m/44'/195'/0'/0/0",
    ...transaction.raw_data,
});
const sent = await tronWeb.trx.sendRawTransaction({
    ...transaction.raw_data,
    signature: [result.payload.signature],
});
```

### Result

```javascript
{
    success: true,
    payload: {
        signature: string, // signed transaction in hex format
    }
}
```

Error

```javascript
{
    success: false,
    error: {
        message: string // error message
    }
}
```
