import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
    display: flex;
    margin-top: 20px;
`;

const PaymentSuccessFull = () => {
    return <Wrapper>payment success</Wrapper>;
};

export default PaymentSuccessFull;
