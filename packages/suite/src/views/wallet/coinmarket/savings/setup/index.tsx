import React from 'react';
import { withInvityLayout } from '@wallet-components';

const CoinmarketSavingsSetup = () => (
    // TODO: translations
    <>Setup</>
);
export default withInvityLayout(CoinmarketSavingsSetup, {
    showStepsGuide: true,
});
