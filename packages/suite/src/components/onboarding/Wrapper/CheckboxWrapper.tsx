import styled from 'styled-components';

const CheckboxWrapper = styled.div`
    display: flex;
    flex-direction: row;
    margin-top: 2px;
    margin-bottom: 2px;
`;

export default CheckboxWrapper;
