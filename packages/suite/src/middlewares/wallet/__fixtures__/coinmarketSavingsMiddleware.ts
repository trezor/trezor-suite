import type { SavingsTradeResponse } from '@suite-services/invityAPI';

export const SAVINGS_TRADE_RESPONSE = {
    trade: {
        exchange: 'fake',
        kycStatus: 'Open',
    },
} as SavingsTradeResponse;
