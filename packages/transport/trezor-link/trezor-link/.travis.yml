sudo: false
language: node_js

node_js:
  - "4.4"

before_script:
  - npm install -g eslint eslint-plugin-flow-vars babel-eslint eslint-plugin-standard eslint-plugin-promise flow-bin

script:
  - make check
  - make .move-in-node build .cleanup-node
  - make .move-in-browser build .cleanup-browser
  - make .move-in-browser-extension build .cleanup-browser-extension
