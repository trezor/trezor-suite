import { Loader as LoaderComponent } from './Loader';

export default {
    title: 'Loaders/Loader',
    component: LoaderComponent,
};

export const Loader = {
    args: {
        size: 50,
        strokeWidth: 1,
    },
};
