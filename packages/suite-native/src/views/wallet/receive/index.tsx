import React from 'react';
import { Text } from 'react-native';
import Layout from '@suite-components/Layout';

const WalletReceive = () => {
    return (
        <Layout title="Wallet/Receive">
            <Text>Account receive</Text>
        </Layout>
    );
};

export default WalletReceive;
