const makePdf = (_definitions: any): Promise<any> => Promise.resolve();

const prepareContent = (_data: any) => {
    return {};
};

const prepareCsv = (_fields: any, _content: any[]) => {
    return '';
};

const preparePdf = (_fields: any, _content: any[], _coin: any) => {
    return {};
};

export const formatData = (_data: any): Promise<Blob> => {
    return Promise.resolve(new Blob());
};
