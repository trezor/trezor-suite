export const saveWalletSettings = async (_settings: any) => {
    return Promise.resolve({});
};

export const getWalletSettings = async () => {
    return Promise.resolve({});
};

export const saveSuiteSettings = async (_settings: any) => {
    return Promise.resolve({});
};

export const getSuiteSettings = async () => {
    return Promise.resolve({
        language: 'en',
    });
};
