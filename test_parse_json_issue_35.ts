// Wait. A "blank screen" in React Native occurs when a React Component throws an error during render, and there's no ErrorBoundary, or the ErrorBoundary just returns a blank view!
// What component renders `ethereumSignTypedData`?
