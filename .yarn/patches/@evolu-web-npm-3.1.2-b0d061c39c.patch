diff --git a/dist/src/local-first/Shared.worker.js b/dist/src/local-first/Shared.worker.js
index 975775eb64c84ca234b8e584fb56376ed99be33e..a006b39ce043ac42e59d10e0fb78d6f0027a1150 100644
--- a/dist/src/local-first/Shared.worker.js
+++ b/dist/src/local-first/Shared.worker.js
@@ -57,9 +57,51 @@ import { createWebSocket, ok, waitForAbort } from "@evolu/common";
 import { initSharedWorker } from "@evolu/common/local-first";
 import { createRun } from "../Task.js";
 import { createOneTabSharedWorkerSelfPolyfill, createSharedWorkerSelf, createWorkerDeps, } from "../Worker.js";
+const workerDeps = createWorkerDeps();
+const serializeWebSocketError = (error) => ({
+    type: error.type,
+    ...(error.type === "RetryError" && {
+        attempts: error.attempts,
+        cause: { type: error.cause.type, eventType: error.cause.event.type },
+    }),
+    ...(error.type !== "RetryError" && { eventType: error.event.type }),
+});
+const createDebugWebSocket = (url, options = {}) => {
+    workerDeps.console.info("webSocketCreated", { url });
+    return async (run) => {
+        const result = await run(createWebSocket(url, {
+            ...options,
+            onOpen: () => {
+                workerDeps.console.info("webSocketOpen", { url });
+                options.onOpen?.();
+            },
+            onClose: (event) => {
+                workerDeps.console.info("webSocketClose", { url, code: event.code });
+                options.onClose?.(event);
+            },
+            onError: (error) => {
+                workerDeps.console.info("webSocketError", {
+                    url,
+                    error: serializeWebSocketError(error),
+                });
+                options.onError?.(error);
+            },
+        }));
+        if (!result.ok)
+            return result;
+        const webSocket = result.value;
+        return ok({
+            ...webSocket,
+            [Symbol.asyncDispose]: async () => {
+                await webSocket[Symbol.asyncDispose]();
+                workerDeps.console.info("webSocketDestroyed", { url });
+            },
+        });
+    };
+};
 const run = createRun({
-    ...createWorkerDeps(),
-    createWebSocket,
+    ...workerDeps,
+    createWebSocket: createDebugWebSocket,
     lockManager: navigator.locks,
 });
 void run(async (run) => {
diff --git a/src/local-first/Shared.worker.ts b/src/local-first/Shared.worker.ts
index 55819f8a63348d978e4e1c8cfe91028298743b2c..9d7d935c60ad6d0980175b4ef616e70c8a80f91b 100644
--- a/src/local-first/Shared.worker.ts
+++ b/src/local-first/Shared.worker.ts
@@ -4,7 +4,13 @@ declare const self: DedicatedWorkerGlobalScope | SharedWorkerGlobalScope;
 import { installPolyfills } from "@evolu/common/polyfills";
 installPolyfills();
 
-import { createWebSocket, ok, waitForAbort } from "@evolu/common";
+import {
+  createWebSocket,
+  ok,
+  waitForAbort,
+  type CreateWebSocket,
+  type WebSocketError,
+} from "@evolu/common";
 import type {
   SharedWorkerInput,
   SharedWorkerOutput,
@@ -18,9 +24,59 @@ import {
   createWorkerDeps,
 } from "../Worker.ts";
 
+const workerDeps = createWorkerDeps();
+
+const serializeWebSocketError = (error: WebSocketError) => ({
+  type: error.type,
+  ...(error.type === "RetryError" && {
+    attempts: error.attempts,
+    cause: { type: error.cause.type, eventType: error.cause.event.type },
+  }),
+  ...(error.type !== "RetryError" && { eventType: error.event.type }),
+});
+
+const createDebugWebSocket: CreateWebSocket = (url, options = {}) => {
+  workerDeps.console.info("webSocketCreated", { url });
+
+  return async (run) => {
+    const result = await run(
+      createWebSocket(url, {
+        ...options,
+        onOpen: () => {
+          workerDeps.console.info("webSocketOpen", { url });
+          options.onOpen?.();
+        },
+        onClose: (event) => {
+          workerDeps.console.info("webSocketClose", { url, code: event.code });
+          options.onClose?.(event);
+        },
+        onError: (error) => {
+          workerDeps.console.info("webSocketError", {
+            url,
+            error: serializeWebSocketError(error),
+          });
+          options.onError?.(error);
+        },
+      }),
+    );
+
+    if (!result.ok) return result;
+
+    const webSocket = result.value;
+
+    return ok({
+      ...webSocket,
+      [Symbol.asyncDispose]: async () => {
+        await webSocket[Symbol.asyncDispose]();
+        workerDeps.console.info("webSocketDestroyed", { url });
+      },
+    });
+  };
+};
+
 const run = createRun({
-  ...createWorkerDeps(),
-  createWebSocket,
+  ...workerDeps,
+  createWebSocket: createDebugWebSocket,
   lockManager: navigator.locks,
 });
 
