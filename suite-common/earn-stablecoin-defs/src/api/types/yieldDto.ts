/**
 * Generated by orval v8.8.0 🍺
 * Do not edit manually.
 * Trezor Earn Yield API
 * OpenAPI spec version: 0.1.0
 */
import type { TokenDto } from './tokenDto';
import type { YieldDtoCurator } from './yieldDtoCurator';
import type { YieldDtoExecutionContracts } from './yieldDtoExecutionContracts';
import type { YieldDtoMechanics } from './yieldDtoMechanics';
import type { YieldDtoMetadata } from './yieldDtoMetadata';
import type { YieldDtoNetwork } from './yieldDtoNetwork';
import type { YieldDtoOutputToken } from './yieldDtoOutputToken';
import type { YieldDtoRewardRate } from './yieldDtoRewardRate';
import type { YieldDtoRisk } from './yieldDtoRisk';
import type { YieldDtoState } from './yieldDtoState';
import type { YieldDtoStatistics } from './yieldDtoStatistics';
import type { YieldDtoStatus } from './yieldDtoStatus';
import type { YieldDtoToken } from './yieldDtoToken';

export type YieldDto = {
    /** Unique identifier for this yield opportunity */
    id: string;
    /** Network this yield opportunity is on */
    network: YieldDtoNetwork;
    /**
     * EVM chain ID for this network (only for EVM networks)
     * @nullable
     */
    chainId?: number | null;
    /** Accepted input tokens (auto-converted as needed) */
    inputTokens: TokenDto[];
    /** Token received from the protocol */
    outputToken?: YieldDtoOutputToken;
    /** Canonical deposit token - used for balances */
    token: YieldDtoToken;
    /** Canonical deposit tokens - used for balances */
    tokens: TokenDto[];
    /** Total effective yield broken down by source and token. */
    rewardRate: YieldDtoRewardRate;
    /** Key statistics and analytics for this yield opportunity */
    statistics?: YieldDtoStatistics;
    /** Top-level provider risk ratings for this yield. */
    risk?: YieldDtoRisk;
    /** Current availability of user actions like enter, exit, claim */
    status: YieldDtoStatus;
    /** Descriptive metadata including name, logo, description, and documentation */
    metadata: YieldDtoMetadata;
    /** Operational mechanics including constraints, fees, and capabilities */
    mechanics: YieldDtoMechanics;
    /** The provider ID this yield belongs to */
    providerId: string;
    /** Curator information for the yield (if applicable) */
    curator?: YieldDtoCurator;
    /** Optional tags for filtering or categorization */
    tags?: string[];
    state?: YieldDtoState;
    /** Per-action set of on-chain contract addresses that transactions may target (tx.to). Used by policy-enforced custody providers to whitelist execution destinations. */
    executionContracts?: YieldDtoExecutionContracts;
};
