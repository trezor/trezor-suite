## Suite Sync Types

This package is the abstraction for the whole Suite Sync. Those types are then
used in Dependency Injection and `ExtraDependencies` in Web/Desktop/Native.

Having those types separated prevents circular dependencies.
