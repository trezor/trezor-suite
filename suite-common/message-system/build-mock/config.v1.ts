// Loaded by suite/desktop-app-renderer/webpack.config.ts when TEST_BUILD=true,
// replacing files/config.v1.ts so the desktop test build does not depend on the signed
// production config.
export { validJws as jws } from '../src/__fixtures__/messageSystemActions';
