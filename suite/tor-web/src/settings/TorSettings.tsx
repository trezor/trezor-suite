// Web has no Tor daemon to configure, so there are no web-specific Tor settings yet.
// Kept as a placeholder so the shared settings screen renders a platform component symmetrically;
// future web-only Tor settings belong here.
export const TorSettings = () => null;
