# Form components

A general package for commonly used form components across the codebase to prevent boilerplate and duplication, and mainly to have a single point of maintenance, built on top of `react-hook-form`.
