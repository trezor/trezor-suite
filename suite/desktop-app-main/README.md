# @suite/desktop-app-main

Native modules and build scripts for Trezor Suite desktop application.

The reason why this package is separated from @suite/desktop-app is to get all non-electron dependencies bundled and prevent unused dependencies from being copied into build.
