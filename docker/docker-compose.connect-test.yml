services:
  trezor-user-env-unix:
    image: ghcr.io/trezor/trezor-user-env:929ac0b17a53e5cec4d1a55a4b12b5a1007f3c6e
    environment:
      - SDL_VIDEODRIVER=dummy
      - XDG_RUNTIME_DIR=/var/tmp
    volumes:
      - ../:/trezor-suite
    network_mode: host
