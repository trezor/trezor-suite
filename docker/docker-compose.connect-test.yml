services:
  trezor-user-env-unix:
    image: ghcr.io/trezor/trezor-user-env:4151e8135a5726d451c0cbeca42ab3b62b6ab4b1
    environment:
      - SDL_VIDEODRIVER=dummy
      - XDG_RUNTIME_DIR=/var/tmp
    volumes:
      - ../:/trezor-suite
    network_mode: host
