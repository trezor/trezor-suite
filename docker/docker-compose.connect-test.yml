services:
  trezor-user-env-unix:
    image: ghcr.io/trezor/trezor-user-env:0126ecbed6edc132a4210803707a66fd04add700
    environment:
      - SDL_VIDEODRIVER=dummy
      - XDG_RUNTIME_DIR=/var/tmp
    volumes:
      - ../:/trezor-user-env/trezor-suite
    network_mode: host
