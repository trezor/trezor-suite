image: node:8

cache:
  paths:
  - node_modules

before_script:
  - yarn install

stages:
  - test
  - build
  - deploy

lint:
  stage: test
  script:
    - yarn run lint

flow:
  stage: test
  script:
    - yarn run flow

test:
  stage: test
  script:
    - yarn run test

build development:
  stage: build
  script:
    - yarn run build:dev
  artifacts:
    expire_in: 1 month
    paths:
      - /build

build beta:
  stage: build
  script:
    - yarn run build:beta
  artifacts:
    expire_in: 1 month
    paths:
      - /build

build production:
  stage: build
  script:
    - yarn run build:prod
  artifacts:
    expire_in: 1 month
    paths:
      - /build

# deploy
deploy review:
  stage: deploy
  script:
    - echo "Deploy a review app"
  environment:
    name: review/$CI_COMMIT_REF_NAME
    url: https://$CI_ENVIRONMENT_SLUG.example.com
  only:
    - branches
  except:
    - master