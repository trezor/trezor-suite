## Description

- Route exchange trades with `status: 'ERROR'` and an `orderId` to the detail page instead of showing only a toast.
- Persist the failed exchange trade and transaction id so the detail page shows the existing failed state.
- Keep request-level confirm errors on the form as a toast so the user can retry.

## Notes for QA

- On Swap, make a DEX trade fail after send or return `status: 'ERROR'`, and verify Trade detail shows `Transaction failed` with `Back to Swap`.
- On Swap, trigger a recoverable confirm error before an order is created, and verify the form stays open with a toast.

## Related Issue

Resolve #24395

## Screenshots:

N/A
